# TASK-0002 — Credential Exposure Containment

## Authority & Metadata
- **Task ID:** TASK-0002
- **Title:** Credential Exposure Containment
- **Priority:** CRITICAL
- **Category:** Security / Secret Management / Credential Integrity
- **Owner:** Chief Software Architect / Project Supervisor (ChatGPT)
- **Implementation:** Technical Contributor (Gemini/Antigravity)
- **Status:** IN_REVIEW
- **Base Branch:** `main`
- **Implementation Branch:** `ai-eos/task-0002-credential-exposure-containment`

---

## 1. Objective
Eliminate credential and secret exposure risks across the repository without modifying production secrets, rewriting git history, or breaking core application functionality. Secure database pool connections with verified TLS, restrict privileged administrative clients to server-side execution, eliminate unsafe raw SQL manipulations of `auth.users`, and provide regression testing for previous security boundaries.

---

## 2. Invariants & Acceptance Criteria
- **AC-001 (Zero Tracked Production Credentials):** No active or historical plaintext database credentials, Supabase pooler connection strings, or service-role keys may remain in the tracked working tree.
- **AC-002 (Elimination of Ad-Hoc Migration & Debug Scripts):** Remove all unvetted root and debug scripts containing hardcoded credentials or dangerous database operations (`run_migration_*.js`, `test-create-session.ts`, `dump_applicants.js`, `src/scripts/*`).
- **AC-003 (Untracking of Scratch Artifacts):** Untrack `scratch/` directory, purge committed temporary scripts, and configure `.gitignore` to prevent future tracking.
- **AC-004 (Sanitized Maintenance Scripts):** Utility and diagnostic scripts under `scripts/` must source credentials from environment variables (`DATABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`) and must never log plaintext credentials.
- **AC-005 (Secure Database TLS Enforcement):** All PostgreSQL connections across `src/` and `scripts/` must enforce secure TLS validation (`rejectUnauthorized: true`). `rejectUnauthorized: false` is forbidden.
- **AC-006 (Fail-Closed Environment Configuration):** Environment variables required for privileged operations must be strictly validated; missing configurations must throw clear errors rather than falling back to default credentials.
- **AC-007 (Server-Only Administrative Isolation):** `src/lib/supabase/admin.ts` and `src/lib/db/pg-fallback.ts` must enforce server-only isolation via `import 'server-only'` to prevent bundling into client components.
- **AC-008 (Elimination of Raw auth.users SQL Mutations):** Direct SQL insertions, updates, or password overrides targeting `auth.users` must be completely removed in favor of Supabase Auth Admin APIs.
- **AC-009 (Automated Security Test Suite):** A dedicated regression suite `tests/security/credential-containment.test.ts` (SEC-01 through SEC-18) must validate security properties across the repository.
- **AC-010 (Admin Client Integrity):** Verify `src/lib/supabase/admin.ts` is server-only, fail-closed, and cannot expose the Supabase service-role credential to client code.
- **AC-011 (Service-Role Import Audit):** Enumerate every `createAdminClient`, `SUPABASE_SERVICE_ROLE_KEY`, and `DATABASE_URL` consumer in the repository and classify each as server-safe or requiring remediation.
- **AC-012 (No Unnecessary PostgreSQL Fallback):** Removing raw `auth.users` password manipulation must not introduce unnecessary `getPgPool()` usage. Remove dangerous login mutations without adding new direct-database dependencies.
- **AC-013 (Authentication Flow Integrity):** Registration, login, provisioning, password reset, and profile synchronization must use Supabase Auth APIs for authentication state. Profile synchronization on auth callback must never trust user-controlled `user_metadata.role` or `user_metadata.tenant_id` as authoritative data. No route may directly manipulate `auth.users` credentials via SQL.
- **AC-014 (Historical Secret Distinction):** The implementation report must distinguish: (1) current-tree remediation, (2) exposed Git history, (3) credential rotation, and (4) deployment-secret replacement.
- **AC-015 (Static Security Tests):** Tests SEC-01 through SEC-18 must test concrete security properties rather than superficial keyword matching.
- **AC-016 (RLS Preservation):** Do not weaken or bypass existing RLS policies to make credential/authentication changes work.
- **AC-017 (Previous-Task Regression):** Verify TASK-0004 `authorizeApiRequest()` and TASK-0005 privileged API containment continue to function after TASK-0002 changes.
- **AC-018 (Server-Authoritative Invitation Record):** Dedicated `public.user_invitations` table must record server-authoritative invitation identity, invited email, authoritative tenant, authoritative role, expiration, issuer, and consumed/accepted state with uniqueness constraints on pending invitations.
- **AC-019 (Role Hierarchy for Invitations):** Invitations must be created only after the requesting administrator passes the existing authorization hierarchy (`super_admin` platform-wide, `org_admin` within org and child schools, `school_admin` within own school).
- **AC-020 (Replay & Expiration Defense):** Invitation acceptance must bind strictly to the authenticated GoTrue `user.id`. Consumed invitations cannot be replayed, expired invitations are rejected, and rebinding to a second user is blocked.
- **AC-021 (Atomic Profile Binding & Fail-Closed Errors):** Role and tenant must come exclusively from the server-authoritative invitation record. Profile binding must be atomic; eliminate destructive `delete + insert` patterns. Database lookups and mutations must fail closed without deleting or corrupting existing records.
- **AC-022 (Expanded Security Test Suite):** Security regression tests SEC-19 through SEC-28 must validate all invitation trust boundary properties, bringing total credential containment tests to 29 and repository-wide test count to 66.
- **AC-023 (True PostgreSQL Atomicity — CORRECTION-03):** Profile creation and invitation consumption must execute inside a single PostgreSQL transaction via a `SECURITY DEFINER` stored procedure (`bind_invitation_to_user`). Application-level compensating deletes are forbidden.
- **AC-024 (SELECT FOR UPDATE Concurrency — CORRECTION-03):** The stored procedure must acquire a row-level lock (`SELECT ... FOR UPDATE`) on the invitation row to prevent concurrent acceptance races.
- **AC-025 (No Privilege Parameters — CORRECTION-03):** The RPC must not accept `role`, `tenant_id`, `email`, or `full_name` as authoritative parameters. Those values must come exclusively from the locked invitation row.
- **AC-026 (SECURITY DEFINER Hardening — CORRECTION-03):** The function must use a fixed `search_path = public, pg_catalog`, revoke EXECUTE from PUBLIC/anon/authenticated, and grant EXECUTE only to `service_role`.
- **AC-027 (Token Column Documented — CORRECTION-03):** The `user_invitations.token` column must be documented as currently unused. No bearer credential may be generated, stored, returned, or logged. A follow-up migration (REC-0014) should remove the column.
- **AC-028 (GoTrue Correlation Documented — CORRECTION-03):** The correlation between GoTrue authentication and the application invitation must be explicitly documented in `callback-sync.ts`. Email equality alone is not authorization; the authoritative `user_invitations` record is required.
- **AC-029 (Expanded Test Suite SEC-29 to SEC-38 — CORRECTION-03):** Ten new security tests must be added covering transactional binding, concurrent acceptance (with explicit limitation disclosure), token security, GoTrue correlation, metadata manipulation, replay, expiration, revocation, cross-tenant abuse, and identity conflict. Total: 39 credential-containment tests, 76 repository-wide.
- **AC-030 (Rollback Test — CORRECTION-03):** At least one test must verify that an RPC failure leaves the invitation in `pending` state and no profile is created.
- **AC-031 (Existing Profile Pass-Through Safety — CORRECTION-03):** The existing-profile pass-through path must be documented and must reject invitations with a conflicting tenant or role for an already-established identity.

---

## 3. Non-Goals
- Do not rotate production credentials (human administrator action required).
- Do not rewrite Git history (human action required prior to public release).
- Do not redesign RBAC or RLS architectures.
- Do not implement multi-factor authentication (MFA).
- Do not rewrite the authentication architecture.
- Do not modify unrelated modules.
- Do not merge the implementation branch into `main`.
