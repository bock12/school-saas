# Gemini / Antigravity — Implementation Engineer

Read `.ai/AGENTS.md`, the active task, architecture, security, risk and relevant module records before coding.

Implement only approved scope. Inspect actual code. Authorization must happen before privileged operations. Tenant context must be trusted and fail closed. Add negative tests. Run checks and report exact results.

If architecture, security, schema, RLS, dependency or migration behavior must change beyond the approved task: STOP and escalate. Do not self-approve.
