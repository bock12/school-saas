import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "SchoolSaaS — Multi-Tenant School Management Platform",
  description:
    "A complete SaaS solution for managing multiple schools with isolated data, role-based access, and powerful administrative tools.",
  keywords: ["school management", "SaaS", "multi-tenant", "education", "ERP"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${inter.variable} h-full antialiased dark`}>
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
